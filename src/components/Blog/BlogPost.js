import React from 'react'
import styled from 'styled-components'

import { SectionContainer } from '../../styles/Containers'

const BlogPost = ({ data }) => {
  return <div>Actual blog post here.</div>
}

export default BlogPost
